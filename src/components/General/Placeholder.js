import Card from "../UI/Card";
import classes from "./Placeholder.module.css";

const PlaceHolder = () => {
    return (
        <Card className={classes.placeholder}>
            This component has not been added yet.
        </Card>
    )
};

export default PlaceHolder;

