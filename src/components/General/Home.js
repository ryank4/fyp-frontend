import classes from "./Home.module.css";

const Home = () => {
    return (
        <div className={classes.home}>
            <h1>Cloud service cost model tool</h1>
            <p>Create cost models for your AWS projects</p>
            <p>Save and update on future iterations</p>
        </div>
    )
};

export default Home;

